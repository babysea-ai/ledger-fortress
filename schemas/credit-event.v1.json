{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ledger-fortress.dev/schemas/credit-event.v1.json",
  "title": "CreditEvent",
  "description": "One row per credit lifecycle event. Logged to the credit_ledger table on every balance or settlement event.",
  "type": "object",
  "required": [
    "schema_version",
    "event_id",
    "account_id",
    "type",
    "amount",
    "balance_after",
    "occurred_at"
  ],
  "properties": {
    "schema_version": {
      "const": "credit-event.v1",
      "description": "Always 'credit-event.v1' for this schema."
    },
    "event_id": {
      "type": "string",
      "description": "Globally unique id (UUID) for the ledger entry.",
      "minLength": 1
    },
    "account_id": {
      "type": "string",
      "description": "Account identifier (UUID).",
      "minLength": 1
    },
    "type": {
      "type": "string",
      "description": "The type of credit event.",
      "enum": [
        "reserve",
        "charge",
        "refund",
        "add",
        "clawback",
        "trueup",
        "uncollectible"
      ]
    },
    "amount": {
      "type": "number",
      "description": "Credit amount involved in this event. True-up events may be positive, negative, or zero. Clawback events may be zero when the balance was already exhausted. Other event types are positive."
    },
    "balance_after": {
      "type": "number",
      "description": "Account balance after this event was applied.",
      "minimum": 0
    },
    "generation_id": {
      "type": ["string", "null"],
      "description": "Links generation settlement events to a specific generation. Required for charge, refund, and true-up events. Optional for reserve."
    },
    "model": {
      "type": ["string", "null"],
      "description": "Model identifier for audit trail (e.g. 'flux-schnell')."
    },
    "description": {
      "type": ["string", "null"],
      "description": "Human-readable note. For 'add' events, this is the idempotency key (e.g. 'invoice:inv_xxx')."
    },
    "occurred_at": {
      "type": "string",
      "format": "date-time",
      "description": "RFC 3339 timestamp of when the event occurred."
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "type": { "const": "clawback" }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "amount": { "minimum": 0 }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "enum": ["reserve", "charge", "refund", "add", "uncollectible"]
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "amount": { "exclusiveMinimum": 0 }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": { "enum": ["charge", "refund", "trueup"] }
        },
        "required": ["type"]
      },
      "then": {
        "required": ["generation_id"],
        "properties": {
          "generation_id": { "type": "string", "minLength": 1 }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": { "enum": ["add", "clawback"] }
        },
        "required": ["type"]
      },
      "then": {
        "required": ["description"],
        "properties": {
          "description": { "type": "string", "minLength": 1 }
        }
      }
    }
  ],
  "additionalProperties": false
}
